"""MCP semantic Blender tools.

Focused semantic tools only (move_object, and later resize_object, create_wall,
set_material, ...). Arbitrary execute_python is NOT the primary interface and is
not exposed at all (see .kiro/steering/security.md).
"""
